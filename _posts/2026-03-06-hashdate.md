---
layout: post
title: hashdate
title_en: hashdate
date: 2020-05-14
category: kode
category_en: code
type: static
cover: /assets/screenshots/hashdate-ezgif.com-optimize.gif
image: /assets/screenshots/hashdate-ezgif.com-optimize.gif
links:
  - label: repo
    url: https://github.com/sloev/hashdate
body_en: |-
  Turns Python datetimes (or iso dates with `cli`) into hashes.

  The hashes support shortening to reduce precision, so a prefix of a hash will be the same datetime rounded to the given hashlength.
---
Turns Python datetimes (or iso dates with `cli`) into hashes.

The hashes support shortening to reduce precision, so a prefix of a hash will be the same datetime rounded to the given hashlength.
